import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    headerText: {
        paddingTop: 10,
        textAlign: 'center',
        color: 'grey'
  },

});

export default styles;
